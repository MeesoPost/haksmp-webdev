---
import { plugins, modpacks, modpackInfo } from '../data/plugins.js';
---

<div class="utrecht-wrapper">
<utrecht-document>
  <utrecht-page-header>
    <utrecht-heading level="1" class="page-title">Mods & Plugins
  </utrecht-page-header>
   <utrecht-paragraph lead>
      Alle mods en plugins die we draaien op Hak SMP Season 4.
    </utrecht-paragraph>

  <utrecht-page-content>
    <section class="modpack-info">
      <utrecht-heading level="2" class="modpack-name-heading">{modpackInfo.name}</utrecht-heading>
      <utrecht-data-list class="modpack-details">
        <utrecht-data-list-item>  
          <utrecht-data-list-key>Modpack versie</utrecht-data-list-key>
          <utrecht-data-list-value>{modpackInfo.version}</utrecht-data-list-value>
        </utrecht-data-list-item>
        <utrecht-data-list-item>
          <utrecht-data-list-key>Minecraft versie</utrecht-data-list-key>
          <utrecht-data-list-value>{modpackInfo.minecraftVersion}</utrecht-data-list-value>
        </utrecht-data-list-item>
        <utrecht-data-list-item>
          <utrecht-data-list-key>Forge versie</utrecht-data-list-key>
          <utrecht-data-list-value>{modpackInfo.forgeVersion}</utrecht-data-list-value>
        </utrecht-data-list-item>
        <utrecht-data-list-item>
          <utrecht-data-list-key>Aantal mods</utrecht-data-list-key>
          <utrecht-data-list-value>{modpackInfo.totalMods}</utrecht-data-list-value>
        </utrecht-data-list-item>
        <utrecht-data-list-item>
          <utrecht-data-list-key>Laatst bijgewerkt</utrecht-data-list-key>
          <utrecht-data-list-value>{modpackInfo.lastUpdated}</utrecht-data-list-value>
        </utrecht-data-list-item>
      </utrecht-data-list>
      </section>

        <utrecht-heading level="3">Download Modpack</utrecht-heading>

      <section class="download-section">
        <utrecht-paragraph class="download-description">
          Download hier de modpack met alle mods en plugins die je hieronder kan zien. instructies vind je in de <u>Discord</u>.
        </utrecht-paragraph>
        
        <utrecht-paragraph class="modpack-explanation">
          * Performance Pack is met leifde gemaakt voor wie een iets mindere video game machine heeft &lt;3
        </utrecht-paragraph>
        
        <div class="button-group">
          {modpacks.map((modpack) => (
            <div class="download-item">
              <button 
                type="button"
                class={`btn-discord btn-download btn-download--${modpack.type}`}
              >
                <svg class="download-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                  <polyline points="7,10 12,15 17,10"/>
                  <line x1="12" y1="15" x2="12" y2="3"/>
                </svg>
                <span class="button-content">
                  <span class="button-title">
                    {modpack.type === 'enhanced' ? 'Download Modpack' : 'Download Performance Pack'}
                  </span>
                </span>
              </button>
              <utrecht-badge appearance="info" class="version-badge">v{modpack.version}</utrecht-badge>
            </div>
          ))}
        </div>


      </section>
      
      <section class="plugins-list">
        <utrecht-heading level="2">Alle mods en plugins</utrecht-heading>
        
        <div class="pack-legend">
          <span class="legend-text">Versions:</span>
          <span class="legend-item">
            <span class="pack-indicator pack-indicator--normal">
              <i class="ti ti-circle-check"></i>
            </span>
            normal
          </span>
          <span class="legend-item">
            <span class="pack-indicator pack-indicator--performance">
              <i class="ti ti-bolt"></i>
            </span>
            Performance
          </span>
        </div>
          
          <div class="plugins-table">
            <utrecht-table>
              <utrecht-table-caption>
                Hier staan alle mods en plugins
              </utrecht-table-caption>
              <utrecht-table-header>
                <utrecht-table-row>
                  <utrecht-table-header-cell>Mod/Plugin</utrecht-table-header-cell>
                  <utrecht-table-header-cell>Beschrijving</utrecht-table-header-cell>
                  <utrecht-table-header-cell>Versie</utrecht-table-header-cell>
                  <utrecht-table-header-cell>Packs</utrecht-table-header-cell>
                  <utrecht-table-header-cell>Status</utrecht-table-header-cell>
                </utrecht-table-row>
              </utrecht-table-header>
              <utrecht-table-body>
                {plugins.map((plugin) => (
                  <utrecht-table-row>
                    <utrecht-table-header-cell class="plugin-name">
                      {plugin.name}
                      <div class="plugin-author">{plugin.author}</div>
                    </utrecht-table-header-cell>
                    <utrecht-table-cell>
                      <utrecht-paragraph class="plugin-description">{plugin.description}</utrecht-paragraph>
                    </utrecht-table-cell>
                    <utrecht-table-cell>
                      <utrecht-code class="version-code">{plugin.version}</utrecht-code>
                    </utrecht-table-cell>
                    <utrecht-table-cell>
                      <div class="pack-indicators">
                        {plugin.inNormalPack && (
                          <span class="pack-indicator pack-indicator--normal" title="In Normal Pack">
                            <i class="ti ti-circle-check"></i>
                          </span>
                        )}
                        {plugin.inPerformancePack && (
                          <span class="pack-indicator pack-indicator--performance" title="In Performance Pack">
                            <i class="ti ti-bolt"></i>
                          </span>
                        )}
                      </div>
                    </utrecht-table-cell>
                    <utrecht-table-cell>
                      {plugin.required ? (
                        <utrecht-badge appearance="danger">Verplicht</utrecht-badge>
                      ) : (
                        <utrecht-badge appearance="neutral">Optioneel</utrecht-badge>
                      )}
                    </utrecht-table-cell>
                  </utrecht-table-row>
                ))}
              </utrecht-table-body>
            </utrecht-table>
          </div>
        </section>
      </utrecht-page-content>
</utrecht-document>
</div>

<!-- Utrecht components are loaded by UtrechtLoader wrapper -->

<style>
  /* Utrecht Design System web components styling */
  
  /* Global CSS custom properties for web components */
  utrecht-document {
    --utrecht-document-background-color: transparent;
    --utrecht-document-color: #ffffff;
    --utrecht-document-font-family: var(--font-family-primary);
    --utrecht-document-font-size: 1rem;
    --utrecht-document-line-height: 1.6;
    display: block;
    padding: 40px 0;
  }

  /* Page structure */
  utrecht-page-header {
    --utrecht-page-header-margin-block-end: 48px;
    --utrecht-page-header-padding-block-end: 24px;
    --utrecht-page-header-border-block-end-width: 1px;
    --utrecht-page-header-border-block-end-style: solid;
    --utrecht-page-header-border-block-end-color: rgba(255, 255, 255, 0.15);
    display: block;
    margin-bottom: 32px;
    padding-bottom: 24px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.15);
  }

  utrecht-page-content {
    display: block;
  }

  /* Typography with Minecraft font for headings - Using CSS Custom Properties */
  :where(utrecht-heading) {
    --utrecht-heading-font-family: var(--font-family-secondary);
    --utrecht-heading-font-weight: 400;
    --utrecht-heading-line-height: 1.2;
    --utrecht-heading-margin-block-start: 0;
    --utrecht-heading-margin-block-end: 16px;
    --utrecht-heading-color: inherit;
    font-family: var(--font-family-secondary);
    font-weight: 400;
    line-height: 1.2;
    margin-block: 0 16px;
  }

  :where(utrecht-heading[level="1"]) {
    --utrecht-heading-1-font-size: 2.5rem;
    --utrecht-heading-1-color: #00ff88;
    font-size: 2.5rem;
    color: #00ff88;
    text-shadow: 2px 2px 0px rgba(0, 0, 0, 0.8);
  }

  :where(utrecht-heading[level="2"]) {
    --utrecht-heading-2-font-size: 2rem;
    --utrecht-heading-2-color: #ffffff;
    font-size: 2rem;
    color: #ffffff;
    text-shadow: 1px 1px 0px rgba(0, 0, 0, 0.8);
  }

  :where(utrecht-heading[level="3"]) {
    --utrecht-heading-3-font-size: 1.5rem;
    --utrecht-heading-3-color: #ffffff;
    font-size: 1.5rem;
    color: #ffffff;
    text-shadow: 1px 1px 0px rgba(0, 0, 0, 0.8);
  }

  /* Override font for specific modpack name heading */
  .modpack-name-heading {
    --utrecht-heading-font-family: var(--font-family-primary) !important;
    font-family: var(--font-family-primary) !important;
  }

  :where(utrecht-paragraph) {
    --utrecht-paragraph-font-size: 1rem;
    --utrecht-paragraph-line-height: 1.5;
    --utrecht-paragraph-margin-block-start: 0;
    --utrecht-paragraph-margin-block-end: 16px;
    --utrecht-paragraph-color: #e0e0e0;
    font-size: 1rem;
    line-height: 1.5;
    margin-block: 0 16px;
    color: #e0e0e0;
  }

  :where(utrecht-paragraph[lead]) {
    --utrecht-paragraph-lead-font-size: 1.25rem;
    --utrecht-paragraph-lead-color: #b3b3b3;
    font-size: 1.25rem;
    color: #b3b3b3;
  }

  /* Sections */
  section {
    background: transparent;
    display: block;
    margin-bottom: 40px;
  }

  .modpack-info {
    padding: 32px 0;
    border-bottom: 2px solid rgba(255, 255, 255, 0.1);
  }

  .download-section {
    padding: 24px 0 32px 0;
    margin-top: 24px;
    border-bottom: 2px solid rgba(255, 255, 255, 0.1);
  }

  .plugins-list {
    padding: 32px 0 0;
  }

  /* Modpack explanation */
  .modpack-explanation {
    font-size: 0.9rem;
    color: #b3b3b3 !important;
    font-style: italic;
    margin: 24px 0 24px 0;
    display: block;
  }

  /* Button Group - Fix side-by-side layout */
  .button-group {
    display: flex;
    gap: 16px;
    margin: 24px 0;
    flex-wrap: wrap;
    align-items: flex-start;
  }

  /* Download Item wrapper */
  .download-item {
    display: flex;
    flex-direction: column;
    gap: 8px;
    align-items: flex-start;
  }

  /* Version Badge styling */
  .version-badge {
    font-size: 0.7rem;
    padding: 2px 6px;
  }


  /* Data Lists (Utrecht style) */
  utrecht-data-list {
    --utrecht-data-list-margin-block-start: 0;
    --utrecht-data-list-margin-block-end: 0;
    display: flex !important;
    flex-direction: column;
    gap: 12px;
    margin: 0;
  }

  utrecht-data-list-item {
    --utrecht-data-list-item-padding-block-start: 12px;
    --utrecht-data-list-item-padding-block-end: 12px;
    --utrecht-data-list-item-border-block-end-width: 1px;
    --utrecht-data-list-item-border-block-end-style: solid;
    --utrecht-data-list-item-border-block-end-color: rgba(255, 255, 255, 0.08);
    display: flex !important;
    padding: 12px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  utrecht-data-list-key {
    --utrecht-data-list-key-font-weight: 600;
    --utrecht-data-list-key-color: #b3b3b3;
    --utrecht-data-list-key-font-size: 0.9rem;
    font-weight: 600;
    width: 200px;
    flex-shrink: 0;
    color: #b3b3b3;
    font-size: 0.9rem;
    margin: 0;
    display: block;
  }

  utrecht-data-list-value {
    --utrecht-data-list-value-color: #ffffff;
    --utrecht-data-list-value-font-weight: 500;
    color: #ffffff;
    font-weight: 500;
    margin: 0;
    display: block;
  }

  /* Download Section */
  .download-actions {
    display: flex;
    align-items: center;
    gap: 24px;
    flex-wrap: wrap;
  }

  .download-link {
    color: #00ccff;
    text-decoration: underline;
    font-size: 0.9rem;
  }

  .download-link:hover {
    color: #00a3cc;
  }

  /* Utrecht Badges */
  utrecht-badge {
    --utrecht-badge-font-size: 0.75rem;
    --utrecht-badge-font-weight: 600;
    --utrecht-badge-line-height: 1;
    --utrecht-badge-padding-inline-start: 8px;
    --utrecht-badge-padding-inline-end: 8px;
    --utrecht-badge-padding-block-start: 4px;
    --utrecht-badge-padding-block-end: 4px;
    --utrecht-badge-border-radius: 4px;
    --utrecht-badge-text-transform: uppercase;
    --utrecht-badge-letter-spacing: 0.025em;
    display: inline-block !important;
    font-size: 0.75rem;
    font-weight: 600;
    line-height: 1;
    padding: 4px 8px;
    border-radius: 4px;
    text-transform: uppercase;
    letter-spacing: 0.025em;
    white-space: nowrap;
    border: 1px solid transparent;
  }

  utrecht-badge[appearance="danger"] {
    --utrecht-badge-background-color: #dc3545;
    --utrecht-badge-color: #ffffff;
    --utrecht-badge-border-color: #dc3545;
    background-color: #dc3545 !important;
    color: #ffffff !important;
    border-color: #dc3545 !important;
  }

  utrecht-badge[appearance="warning"] {
    --utrecht-badge-background-color: #ffc107;
    --utrecht-badge-color: #000000;
    --utrecht-badge-border-color: #ffc107;
    background-color: #ffc107 !important;
    color: #000000 !important;
    border-color: #ffc107 !important;
  }

  utrecht-badge[appearance="success"] {
    --utrecht-badge-background-color: #198754;
    --utrecht-badge-color: #ffffff;
    --utrecht-badge-border-color: #198754;
    background-color: #198754 !important;
    color: #ffffff !important;
    border-color: #198754 !important;
  }

  utrecht-badge[appearance="info"] {
    --utrecht-badge-background-color: #0dcaf0;
    --utrecht-badge-color: #000000;
    --utrecht-badge-border-color: #0dcaf0;
    background-color: #0dcaf0 !important;
    color: #000000 !important;
    border-color: #0dcaf0 !important;
  }

  utrecht-badge[appearance="neutral"] {
    --utrecht-badge-background-color: #6c757d;
    --utrecht-badge-color: #ffffff;
    --utrecht-badge-border-color: #6c757d;
    background-color: #6c757d !important;
    color: #ffffff !important;
    border-color: #6c757d !important;
  }

  /* Discord-Style Download Buttons */
  .btn-download {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 12px 16px 8px 16px;
    background: var(--discord-blurple);
    color: var(--discord-white);
    border: 3px solid var(--discord-blurple);
    border-radius: 0;
    font-family: var(--font-family-secondary);
    font-weight: 400;
    text-decoration: none;
    cursor: pointer;
    min-height: 44px;
    min-width: 44px;
    box-shadow: 0 4px 0 var(--govuk-shadow);
    position: relative;
    transition: all 0.1s ease;
    user-select: none;
  }

  /* Both buttons use Discord blue - no variants needed */
  .btn-download--enhanced {
    background: var(--discord-blurple);
    border-color: var(--discord-blurple);
  }

  .btn-download--lite {
    background: var(--discord-blurple);
    border-color: var(--discord-blurple);
  }

  /* Hover States - subtle lift */
  .btn-download:hover:not(:disabled) {
    background: var(--discord-dark-blurple, #4752c4);
    transform: translateY(-2px);
    box-shadow: 0 2px 0 var(--govuk-shadow, #0b0c0c);
  }

  /* Active States - pressed into base effect */
  .btn-download:active:not(:disabled) {
    background: var(--discord-dark-blurple, #4752c4);
    transform: translateY(2px);
    box-shadow: 0 2px 0 var(--govuk-shadow, #0b0c0c);
  }

  /* Focus States - keep Discord style */
  .btn-download:focus {
    outline: 2px dotted var(--focus-outline-color);
    outline-offset: 2px;
  }

  .btn-download:focus-visible {
    outline: 2px dotted var(--discord-blurple, #5865f2);
    outline-offset: 2px;
  }

  /* Disabled State */
  .btn-download:disabled {
    background: #2c2f33;
    color: rgba(255, 255, 255, 0.6);
    cursor: not-allowed;
    pointer-events: none;
    transform: translateY(0) !important;
    box-shadow: 0 4px 0 var(--govuk-shadow, #0b0c0c);
  }

  /* Accessibility and Reduced Motion */
  @media (prefers-reduced-motion: reduce) {
    .btn-download:hover:not(:disabled),
    .btn-download:active:not(:disabled) {
      transform: none;
    }
  }

  .download-icon {
    width: 1.25em;
    height: 1.25em;
    fill: none;
    stroke: currentColor;
    stroke-width: 2;
    flex-shrink: 0;
  }

  .button-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
    gap: 1px;
  }

  .button-title {
    font-size: 0.9rem;
    font-weight: 400;
    line-height: 1.1;
    margin: 0;
    color: currentColor;
  }

  .button-subtitle {
    font-size: 0.7rem;
    opacity: 0.8;
    font-weight: 400;
    margin: 0;
    color: currentColor;
  }

  /* Utrecht Tables - Clean styling */
  utrecht-table {
    --utrecht-table-background-color: transparent;
    --utrecht-table-border-color: rgba(255, 255, 255, 0.08);
    --utrecht-table-border-width: 1px;
    --utrecht-table-border-radius: 0;
    width: 100% !important;
    border-collapse: collapse !important;
    border-spacing: 0 !important;
    background: transparent !important;
    border: none !important;
    border-radius: 0 !important;
    overflow: hidden !important;
    display: table !important;
    font-family: var(--font-family-primary) !important;
  }

  utrecht-table-caption {
    --utrecht-table-caption-text-align: left;
    --utrecht-table-caption-font-weight: 600;
    --utrecht-table-caption-color: #b3b3b3;
    --utrecht-table-caption-font-size: 0.9rem;
    --utrecht-table-caption-background-color: rgba(255, 255, 255, 0.02);
    --utrecht-table-caption-border-color: rgba(255, 255, 255, 0.1);
    text-align: left !important;
    font-weight: 600 !important;
    padding: 16px !important;
    color: #b3b3b3 !important;
    font-size: 0.9rem !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1) !important;
    background: rgba(255, 255, 255, 0.02) !important;
    display: table-caption !important;
  }

  utrecht-table-header {
    --utrecht-table-header-background-color: rgba(255, 255, 255, 0.05);
    background: rgba(255, 255, 255, 0.05) !important;
    display: table-header-group !important;
  }

  utrecht-table-body {
    display: table-row-group !important;
  }

  utrecht-table-row {
    display: table-row !important;
  }

  utrecht-table-row:hover {
    background: rgba(255, 255, 255, 0.05) !important;
  }

  utrecht-table-header-cell {
    --utrecht-table-header-cell-font-weight: 600;
    --utrecht-table-header-cell-text-align: left;
    --utrecht-table-header-cell-color: #b3b3b3;
    --utrecht-table-header-cell-font-size: 0.85rem;
    --utrecht-table-header-cell-text-transform: uppercase;
    --utrecht-table-header-cell-letter-spacing: 0.05em;
    --utrecht-table-header-cell-border-color: rgba(255, 255, 255, 0.08);
    font-weight: 600 !important;
    text-align: left !important;
    color: #b3b3b3 !important;
    padding: 12px 16px !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08) !important;
    font-size: 0.85rem !important;
    text-transform: uppercase !important;
    letter-spacing: 0.05em !important;
    display: table-cell !important;
    vertical-align: top !important;
    font-family: var(--font-family-primary) !important;
  }

  utrecht-table-cell {
    --utrecht-table-cell-text-align: left;
    --utrecht-table-cell-color: #e0e0e0;
    --utrecht-table-cell-border-color: rgba(255, 255, 255, 0.05);
    text-align: left !important;
    padding: 14px 16px !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05) !important;
    vertical-align: top !important;
    color: #e0e0e0 !important;
    display: table-cell !important;
    font-family: var(--font-family-primary) !important;
  }

  utrecht-table-row:last-child utrecht-table-cell,
  utrecht-table-row:last-child utrecht-table-header-cell {
    border-bottom: none !important;
  }

  /* Plugin specific styling */
  .plugin-name {
    color: #00ff88 !important;
    font-weight: 600 !important;
    font-size: 0.95rem !important;
    font-family: var(--font-family-primary) !important;
    letter-spacing: 0.01em;
  }

  .plugin-author {
    font-weight: 400;
    color: #999999;
    font-size: 0.75rem;
    margin-top: 4px;
    font-style: normal;
    display: block;
    font-family: var(--font-family-primary);
  }

  .plugin-description {
    margin: 0 !important;
    font-size: 0.85rem;
    line-height: 1.4;
    color: #e0e0e0 !important;
    max-width: 300px;
    font-family: var(--font-family-primary);
  }

  utrecht-code,
  .version-code {
    --utrecht-code-font-family: var(--font-family-primary);
    --utrecht-code-background-color: #2a2d31;
    --utrecht-code-color: #ffffff;
    --utrecht-code-border-color: #3a3d41;
    font-family: var(--font-family-primary) !important;
    background: #2a2d31 !important;
    color: #ffffff !important;
    padding: 4px 8px !important;
    border-radius: 4px !important;
    font-size: 0.75rem !important;
    font-weight: 500 !important;
    border: 1px solid #3a3d41 !important;
    display: inline-block;
    letter-spacing: 0.02em;
  }

  /* Pack Indicators */
  .pack-indicators {
    display: flex;
    gap: 6px;
    align-items: center;
  }

  .pack-indicator {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 20px;
    height: 20px;
    border-radius: 3px;
    transition: all 0.15s ease;
    cursor: help;
  }

  .pack-indicator i {
    font-size: 14px;
    line-height: 1;
  }

  .pack-indicator--normal {
    background: #00ff88;
    color: #000000;
  }

  .pack-indicator--performance {
    background: #ffcc00;
    color: #000000;
  }

  .pack-indicator:hover {
    transform: scale(1.05);
    opacity: 0.8;
  }

  /* Pack Legend */
  .pack-legend {
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 16px;
    font-size: 0.85rem;
    color: #b3b3b3;
    font-family: var(--font-family-primary);
  }

  .legend-text {
    font-weight: 500;
    color: #ffffff;
  }

  .legend-item {
    display: flex;
    align-items: center;
    gap: 6px;
    color: #e0e0e0;
  }

  /* Responsive design */
  @media (max-width: 768px) {
    utrecht-document {
      padding: 24px 0 !important;
    }

    utrecht-heading[level="1"] {
      font-size: 2rem !important;
    }

    utrecht-heading[level="2"] {
      font-size: 1.5rem !important;
    }

    utrecht-heading[level="3"] {
      font-size: 1.25rem !important;
    }

    utrecht-paragraph[lead] {
      font-size: 1.125rem !important;
    }

    .button-group {
      flex-direction: column !important;
      gap: 16px !important;
      align-items: stretch !important;
    }

    .btn-download {
      min-width: auto !important;
      width: 100% !important;
      justify-content: center !important;
      padding: 16px 20px 12px 20px !important;
      min-height: 52px !important;
    }

    .button-content {
      align-items: center !important;
      text-align: center !important;
    }

    .button-title {
      font-size: 1rem !important;
    }

    .button-subtitle {
      font-size: 0.75rem !important;
    }

    utrecht-data-list-item {
      flex-direction: column !important;
      gap: 4px !important;
    }

    utrecht-data-list-key {
      width: auto !important;
      font-size: 0.8rem !important;
    }

    /* Mobile table: Transform to cards */
    utrecht-table-caption,
    utrecht-table-header {
      display: none !important;
    }

    utrecht-table,
    utrecht-table-body,
    utrecht-table-row {
      display: block !important;
      width: 100% !important;
    }

    utrecht-table {
      border: none !important;
      background: transparent !important;
    }

    utrecht-table-row {
      background: rgba(255, 255, 255, 0.05) !important;
      border: 1px solid rgba(255, 255, 255, 0.1) !important;
      border-radius: 8px !important;
      margin-bottom: 16px !important;
      padding: 16px !important;
      display: block !important;
    }

    utrecht-table-header-cell,
    utrecht-table-cell {
      display: block !important;
      border: none !important;
      padding: 8px 0 !important;
      text-align: left !important;
      width: 100% !important;
    }

    utrecht-table-header-cell {
      font-size: 1.1rem !important;
      margin-bottom: 12px !important;
      padding-bottom: 8px !important;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1) !important;
    }

    .plugin-description {
      max-width: none !important;
    }

    .pack-indicators {
      margin-top: 4px;
    }

    .pack-legend utrecht-paragraph {
      flex-direction: column !important;
      align-items: flex-start !important;
      gap: 8px !important;
    }

    .legend-item {
      font-size: 0.8rem;
    }
  }

  @media (max-width: 480px) {
    utrecht-document {
      padding: 16px 0 !important;
    }

    utrecht-page-header {
      margin-bottom: 32px !important;
    }

    .modpack-info,
    .download-section {
      padding: 24px 0;
    }

    .plugins-list {
      padding: 24px 0 0;
    }
  }
</style>